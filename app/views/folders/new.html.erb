<h1>New Folder</h1>
<%= render :partial => "assets/breadcrumbs" %>

<%= render 'form' %> 


  
<p> 
<% if @folder.parent %> 
   <%= link_to "Back to '#{@folder.parent.name}' Folder", browse_path(@folder.parent) %> 
<% else %> 
   <%= link_to "Back to assets page", '/assets' %> 
<% end %> 
</p>
