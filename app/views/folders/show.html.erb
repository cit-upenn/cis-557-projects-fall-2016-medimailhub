<!-- <p id="notice"><%= notice %></p> -->

<p>
  <strong>Name:</strong>
  <%= @folder.name %>
</p>

<!-- <p>
  <strong>Parent:</strong>
  <%= @folder.parent_id %>
</p>

<p>
  <strong>User:</strong>
  <%= @folder.user_id %>
</p> -->
<!-- 
<%= link_to 'Edit', edit_folder_path(@folder) %> | -->
<% if @folder.parent_id %>
	<%= link_to 'Back', browse_path(@folder.parent_id) %>
<% else %>
		<%= link_to 'Back', '/assets' %>
<% end %>