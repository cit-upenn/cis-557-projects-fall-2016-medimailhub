<p id="notice"><%= notice %></p>

<%= image_tag @asset.uploaded_file.url(:thumb) %>

<p>
  <strong>User:</strong>
  <%= @asset.user_id %>
</p>

<%= link_to 'Edit', edit_asset_path(@asset) %> |
<%= link_to 'Back', assets_path %>
